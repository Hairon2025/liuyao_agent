"""卦象显示常量（爻型编码 → 名称 / 卦象符号 / 动爻标记）

这些常量用于把 1-4 编码转换为可读显示文本；
仅在 markdown / 报告渲染时被消费。
"""
from __future__ import annotations

# 爻型编码（1-4 → 显示名称）
LINE_NAMES: dict[int, str] = {
    1: "少阴",
    2: "少阳",
    3: "纯阳",
    4: "纯阴",
}

# 爻型编码（1-4 → 卦象符号）
LINE_SYMBOLS: dict[int, str] = {
    1: "-- --",  # 少阴
    2: "-----",  # 少阳
    3: "-----",  # 纯阳（动）
    4: "-- --",  # 纯阴（动）
}

# 动爻标记
MOVING_MARK: dict[int, str] = {
    3: "  →",
    4: "  →",
    1: "   ",
    2: "   ",
}
